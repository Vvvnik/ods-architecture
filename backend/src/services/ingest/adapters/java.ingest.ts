/**
 * Java native model — symbols schema 1 via shared adapter (018).
 */
import { createSymbolsModelIngestAdapter } from './symbols-model.ingest.js';

export const javaIngestAdapter = createSymbolsModelIngestAdapter('java', 'java');
