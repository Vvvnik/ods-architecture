{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ods.local/ods-help/json-model/es-parser-envelope-storage",
  "title": "ES Document — Stored Parser Envelope",
  "description": "Full document in index ods-parser-envelopes: envelope parser + id ES + stored_at.",
  "implementation_status": "implemented",
  "spec": "005-code-analysis",
  "type": "object",
  "required": ["id", "project_id", "analysis_run_id", "parser_id", "schema_version", "generated_at", "files_analyzed", "model", "stored_at"],
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid",
      "description": "UUID document in Elasticsearch (_id), generates backend upon preservation"
    },
    "project_id": { "$ref": "_shared.schema.json#/$defs/ProjectId" },
    "analysis_run_id": { "$ref": "_shared.schema.json#/$defs/AnalysisRunId" },
    "parser_id": {
      "type": "string",
      "description": "Parser identifier from manifest.json (typescript, compose, …)"
    },
    "schema_version": {
      "type": "string",
      "description": "Version native JSON inside model; ingest selects adapter by parser_id + schema_version"
    },
    "generated_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 UTC; generation moment envelope parser"
    },
    "files_analyzed": {
      "type": "array",
      "description": "List POSIX-paths processed by this parser run",
      "items": {
        "type": "string",
        "description": "Relative path working_copy_root"
      }
    },
    "model": {
      "description": "Native extract parser; schema — see native-*.schema.json by parser_id and schema_version"
    },
    "stored_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 UTC; orchestrator save moment in ods-parser-envelopes"
    }
  }
}
