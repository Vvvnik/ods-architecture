{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ods.local/ods-help/json-model/es-parser-envelope-storage",
  "title": "ES Document — Stored Parser Envelope (metadata only)",
  "description": "Document in index ods-parser-envelopes: parser-run metadata. Native model is NOT stored; orchestrator ingests in-memory chunks into graph indices, then writes this record with model={}.",
  "implementation_status": "implemented",
  "spec": "005-code-analysis",
  "type": "object",
  "required": ["id", "project_id", "analysis_run_id", "parser_id", "schema_version", "generated_at", "files_analyzed", "model", "stored_at"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Stable _id = {analysis_run_id}:{parser_id}"
    },
    "project_id": { "$ref": "_shared.schema.json#/$defs/ProjectId" },
    "analysis_run_id": { "$ref": "_shared.schema.json#/$defs/AnalysisRunId" },
    "parser_id": {
      "type": "string",
      "description": "Parser identifier from manifest.json (typescript, compose, …)"
    },
    "schema_version": {
      "type": "string",
      "description": "Version native JSON used during ingest; adapter key with parser_id"
    },
    "generated_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 UTC; generation moment of last ingested chunk"
    },
    "files_analyzed": {
      "type": "array",
      "description": "Union of POSIX-paths processed across all chunks",
      "items": {
        "type": "string",
        "description": "Relative path working_copy_root"
      }
    },
    "model": {
      "type": "object",
      "description": "Always empty object in ES. Native extract is ephemeral."
    },
    "chunk_count": {
      "type": "integer",
      "minimum": 1,
      "description": "Number of file chunks ingested for this parser in the run"
    },
    "stored_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 UTC; orchestrator save moment in ods-parser-envelopes"
    }
  }
}
