{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ods.local/ods-help/json-model/native-dotnet-api-routes",
  "title": "Native Model — dotnet-api-routes parser",
  "description": "parser_id=dotnet-api-routes, schema_version=1. ASP.NET routes + HTTP client registration.",
  "implementation_status": "planned",
  "spec": "008-code-graph-depth / 009-system-landscape",
  "type": "object",
  "required": ["controllers", "http_clients"],
  "properties": {
    "controllers": {
      "type": "array",
      "description": "ASP.NET Core controllers с Route/Http* attributes",
      "items": {
        "type": "object",
        "required": ["path", "class_name", "routes"],
        "properties": {
          "path": {
            "type": "string",
            "description": "POSIX-путь к файлу контроллера"
          },
          "class_name": {
            "type": "string",
            "description": "Имя C# класса контроллера"
          },
          "base_route": {
            "type": "string",
            "description": "Шаблон [Route] на классе, напр. api/v1/weather"
          },
          "routes": {
            "type": "array",
            "description": "HTTP endpoints контроллера",
            "items": {
              "type": "object",
              "required": ["http_method", "route_template"],
              "properties": {
                "http_method": {
                  "type": "string",
                  "enum": ["GET", "POST", "PUT", "PATCH", "DELETE"],
                  "description": "HTTP verb из HttpGet/HttpPost/… attribute"
                },
                "route_template": {
                  "type": "string",
                  "description": "Шаблон route action: get-data, {id:guid}/upload, …"
                },
                "full_path": {
                  "type": "string",
                  "description": "Собранный URL path: api/v1/weather/forecast"
                },
                "action_method": {
                  "type": "string",
                  "description": "Имя C# action method"
                },
                "location": {
                  "type": "object",
                  "description": "Диапазон строк action в файле контроллера",
                  "properties": {
                    "start_line": {
                      "type": "integer",
                      "description": "Начальная строка action (1-based)"
                    },
                    "end_line": {
                      "type": "integer",
                      "description": "Конечная строка action (1-based)"
                    }
                  }
                }
              }
            }
          }
        }
      }
    },
    "http_clients": {
      "type": "array",
      "description": "Регистрация исходящих HTTP/gRPC клиентов (Refit, HttpClient, …)",
      "items": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "Файл регистрации: Startup.cs, AutofacModule.cs, Program.cs, …"
          },
          "client_type": {
            "type": "string",
            "enum": ["refit", "httpclient", "grpc"],
            "description": "Технология HTTP client"
          },
          "interface": {
            "type": "string",
            "description": "Интерфейс Refit/gRPC client: IUserCredentialsRestApi, …"
          },
          "base_url": {
            "type": "string",
            "description": "BaseAddress или URL literal: http://worker/…, https://api.example.com"
          },
          "target_service_hint": {
            "type": "string",
            "description": "Целевой service или external: worker, external:payments, …"
          }
        }
      }
    }
  }
}
