{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ods.local/ods-help/json-model/es-analysis-run",
  "title": "ES Document — Analysis Run",
  "description": "Document analysis run in index ods-analysis-runs (005; ingest metadata patch discards paths outside",
  "implementation_status": "implemented",
  "spec": "005-code-analysis / 006-project-graph",
  "type": "object",
  "required": ["id", "project_id", "status", "started_at"],
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid",
      "description": "UUID analysis run (analysis_run_id); PK document"
    },
    "project_id": { "$ref": "_shared.schema.json#/$defs/ProjectId" },
    "status": {
      "type": "string",
      "enum": ["pending", "running", "success", "partial", "failed"],
      "description": "Parser orchestration status (005)"
    },
    "started_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 UTC; analysis run start moment"
    },
    "completed_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 UTC; parser completion moment; null so far running"
    },
    "incremental": {
      "type": "boolean",
      "description": "true — analysis of only changed files since last sync"
    },
    "scope_paths": {
      "type": "array",
      "description": "Planned 007/009: POSIX-subfolder prefixes monorepo; null/absent = entire repo",
      "items": {
        "type": "string",
        "description": "Root subfolder scope, e.g. backend/ or user/"
      }
    },
    "ingest_status": {
      "type": "string",
      "enum": ["pending", "running", "success", "partial", "failed"],
      "description": "Status ingest pipeline (006); patch after parsers"
    },
    "ingest_completed_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 UTC; completion moment ingest all adapters"
    },
    "ingest_errors": {
      "type": "array",
      "description": "Errors ingest by parser_id; do not block other adapters",
      "items": {
        "type": "object",
        "required": ["parser_id", "message"],
        "properties": {
          "parser_id": {
            "type": "string",
            "description": "parser_id adapter that failed"
          },
          "message": {
            "type": "string",
            "description": "Error message in Russian for UI"
          }
        }
      }
    }
  }
}
