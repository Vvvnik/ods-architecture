В вашем сообщении не было деталей о subagent — я проверю недавние файлы проекта, чтобы понять, что вам может понадобиться.

Ваше сообщение `/create-subagent` выглядит неполным — в нём нет описания того, что должен делать subagent.

Чтобы создать его, мне нужны несколько деталей:

### 1. Назначение
В чём должен специализироваться этот subagent? Например:
- **Speckit workflow** — запуск `/speckit-specify`, `/speckit-plan` и т.д.
- **Анализ кода** — Roslyn/TypeScript AST, запросы Graphify
- **Архитектурный обзор** — ODS-спецификации, доменная модель, документация AsciiDoc
- **Перевод русский ↔ английский** — документы и спецификации (с учётом `setup-translate.md`)
- Что-то другое

### 2. Область действия
- **На уровне проекта** (`.cursor/agents/` в этом репозитории) — общий для команды, в git
- **На уровне пользователя** (`~/.cursor/agents/`) — личный, доступен во всех проектах

### 3. Поведение
- Когда его вызывать? (триггеры, ключевые слова)
- Какой workflow соблюдать?
- Есть ли ограничения? (язык, формат вывода, используемые инструменты)

---

**Пример запроса, который можно отправить:**

> Создай project-level subagent `speckit-orchestrator`, который ведёт полный Spec Kit workflow (constitution → specify → plan → tasks → implement). Использовать проактивно при старте новой фичи. Следовать skills в `.cursor/skills/speckit-*` и писать спецификации на русском.

Ответьте с назначением, областью действия и поведением (или вставьте черновик промпта) — я создам `.md` файл с frontmatter и system prompt.
