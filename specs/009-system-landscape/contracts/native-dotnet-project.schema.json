{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ods.local/ods-help/json-model/native-dotnet-project",
  "title": "Native Model — dotnet-project parser",
  "description": "parser_id=dotnet-project, schema_version=1. .sln and ProjectReference graph.",
  "implementation_status": "planned",
  "spec": "009-system-landscape",
  "type": "object",
  "required": ["projects"],
  "properties": {
    "solutions": {
      "type": "array",
      "description": "Visual Studio solutions (.sln) in repo",
      "items": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "POSIX-path to .sln to the file"
          },
          "name": {
            "type": "string",
            "description": "Name solution no extension: Sample, WebApplication1"
          }
        }
      }
    },
    "projects": {
      "type": "array",
      "description": "Is everything .csproj and their ProjectReference connections",
      "items": {
        "type": "object",
        "required": ["path", "name"],
        "properties": {
          "path": {
            "type": "string",
            "description": "POSIX-path to .csproj"
          },
          "name": {
            "type": "string",
            "description": "Name project file: Sample.Api.csproj"
          },
          "project_type": {
            "type": "string",
            "enum": ["web", "classlib", "worker", "test", "unknown"],
            "description": "Project type heuristics for Sdk/OutputType"
          },
          "service_folder_hint": {
            "type": "string",
            "description": "Folder deployable service: api, worker, ..."
          },
          "references": {
            "type": "array",
            "description": "ProjectReference → other csproj (fin project_reference)",
            "items": {
              "type": "object",
              "properties": {
                "path": {
                  "type": "string",
                  "description": "Relative POSIX-path target .csproj"
                },
                "name": {
                  "type": "string",
                  "description": "Name target project file"
                }
              }
            }
          },
          "package_references": {
            "type": "array",
            "description": "NuGet PackageReference id (optional versions)",
            "items": {
              "type": "string",
              "description": "Package id: Microsoft.AspNetCore.Mvc, MassTransit.Kafka, …"
            }
          }
        }
      }
    }
  }
}
