{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ods.local/ods-help/json-model/native-bus-kafka",
  "title": "Native Model — bus-kafka parser",
  "description": "parser_id=bus-kafka, schema_version=1. Kafka + MassTransit consumers/publishers.",
  "implementation_status": "planned",
  "spec": "009-system-landscape",
  "type": "object",
  "required": ["handlers"],
  "properties": {
    "service_hint": {
      "type": "string",
      "description": "Logical service out of the way or namespace: Sample.Api, Sample.Worker, …"
    },
    "handlers": {
      "type": "array",
      "description": "Consumer/producer worker classes and MassTransit handlers",
      "items": {
        "type": "object",
        "required": ["path", "role"],
        "properties": {
          "path": {
            "type": "string",
            "description": "POSIX-file path worker/handler"
          },
          "class_name": {
            "type": "string",
            "description": "Name C# class handler, eg. OrderConsumerWorker"
          },
          "role": {
            "type": "string",
            "enum": ["consumer", "producer", "both"],
            "description": "The role of handler relative to Kafka topic"
          },
          "topic": {
            "type": "string",
            "description": "Name Kafka topic if you know of literal or resolved config"
          },
          "topic_config_key": {
            "type": "string",
            "description": "Key appsettings name topic if topic not literal"
          },
          "message_type": {
            "type": "string",
            "description": "CLR type name or schema name payload messages"
          },
          "group_id": {
            "type": "string",
            "description": "Kafka consumer group id; nullable"
          }
        }
      }
    },
    "publish_sites": {
      "type": "array",
      "description": "Places of publication via IBusMessagePublisher.Publish or equivalent",
      "items": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "The file with the call Publish"
          },
          "method": {
            "type": "string",
            "description": "The name of the method C# containing Publish"
          },
          "topic": {
            "type": "string",
            "description": "Name topic of the argument Publish"
          },
          "message_type": {
            "type": "string",
            "description": "Type of published message"
          }
        }
      }
    }
  }
}
