# openapi parser

Parses OpenAPI/Swagger YAML (`openapi*.y*ml`, `contracts/swagger/**`) into native openapi model (`schema_version: 1`).

Extracts paths, HTTP operations, and `info.title` per `specs/009-system-landscape/contracts/native-openapi.schema.json`.
