# dotnet-project parser

Parses `*.sln` and `*.csproj` into native dotnet-project model (`schema_version: 1`).

CLI: `run.sh` → `run.mjs` (XML parse of ProjectReference / PackageReference).

Contract: `specs/009-system-landscape/contracts/native-dotnet-project.schema.json`.
