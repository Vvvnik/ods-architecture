# appsettings parser

Parses `appsettings*.json`, `.env`, and `example.env` into native appsettings model (`schema_version: 1`).

Extracts configuration bindings (`database`, `broker`, …) per `specs/009-system-landscape/contracts/native-appsettings.schema.json`.
